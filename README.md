# E-commerce API (MySQL + Express)

## Setup (copy & paste commands)

1. Open VSCode, create folder `ecommerce-api`, and add files as shown.

2. Put `ecommerce.sql` in a location you can access.

3. Install Node dependencies:
```bash
cd ecommerce-api
npm install
